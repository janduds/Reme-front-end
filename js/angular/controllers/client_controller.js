'user strict';

angular.module('reme.controllers')
		.controller('clientController', clientController);

clientController.$inject = ['$scope', 'clientService', '$location'];

function clientController($scope, clientService, $location) {
	var self = this;
		
}